def square_array(numbers)
  new_numbers = numbers.each { |num| Math.sqrt(num) }
  return new_numbers
end
  
  def square_array(numbers)
  new_numbers = numbers.each do |int|
    Math.sqrt(int)
  return new_numbers
end
